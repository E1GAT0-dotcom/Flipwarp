const guiColors = {
    // The page behind a dialog is dimmed with a dark wash of the accent's own blue.
    'ui-modal-overlay': 'rgba(6, 18, 46, 0.55)',

    'looks-secondary': 'hsla(215, 100%, 65%, 1)',
    'looks-transparent': 'hsla(215, 100%, 65%, 0.35)',
    'looks-light-transparent': 'hsla(215, 100%, 65%, 0.15)',
    'looks-secondary-dark': 'hsla(215, 60%, 50%, 1)'
};

const blockColors = {};

export {
    guiColors,
    blockColors
};
